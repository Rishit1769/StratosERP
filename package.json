{
  "name": "stratoserp",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:web": "turbo dev --filter=@stratoserp/web",
    "dev:api": "turbo dev --filter=@stratoserp/api",
    "build": "turbo build",
    "build:web": "turbo build --filter=@stratoserp/web",
    "build:api": "turbo build --filter=@stratoserp/api",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "typecheck": "turbo typecheck",
    "db:generate": "prisma generate --schema packages/database/prisma/schema.prisma",
    "db:seed": "prisma db seed --schema packages/database/prisma/schema.prisma"
  },
  "prisma": {
    "schema": "packages/database/prisma/schema.prisma",
    "seed": "npm --workspace @stratoserp/database run db:seed"
  },
  "devDependencies": {
    "prettier": "^3.3.0",
    "prisma": "^6.19.3",
    "turbo": "^2.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "npm@10.0.0"
}
